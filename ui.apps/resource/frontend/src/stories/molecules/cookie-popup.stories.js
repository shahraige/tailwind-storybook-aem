import React from 'react';
import CookiePopup from '../../components/molecules/cookie-popup/cookie-popup';

export default {
   title: 'Molecules/CookiePopup',
   component: CookiePopup,
   argTypes: {}
};

const Template = (args) => <CookiePopup  {...args}/>;

export const CookiePopup_default = Template.bind({});

